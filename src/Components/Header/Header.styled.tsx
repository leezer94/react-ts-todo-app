import styled from 'styled-components';

const Header = styled.header`
  margin-top: 1rem;
  margin-bottom: 1rem;
`;

const HomeLink = styled.a`
  color: black;
`;

const Title = styled.h1`
  text-align: center;
  font-weight: 700;
`;

const Nav = styled.nav`
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
`;

export { Header, HomeLink, Title, Nav };
