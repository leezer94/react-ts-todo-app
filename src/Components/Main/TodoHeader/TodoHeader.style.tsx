import styled from 'styled-components';

const Header = styled.div`
  display: flex;
  justify-content: space-between;
`;

const Title = styled.h2`
  margin-top: 0.25rem;
`;

const Span = styled.span`
  margin-right: 0.5rem;
  margin-top: 16px;
`;

export { Header, Title, Span };
